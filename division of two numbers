write a program on division of two numbers
#include <stdio.h>
int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0)
    {
        printf("second no cannot be zero\n");
        b=1;
    }
int r=a/b;
printf("%d",r);
    return 0;
}
